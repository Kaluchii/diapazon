$(document).ready(function () {
    // 1. Initialize fotorama manually.
    var $fotoramaStage = $('.fotorama').fotorama();
    // 2. Get the API object.
    var fotoramaS = $fotoramaStage.data('fotorama');
});